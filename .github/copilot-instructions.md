# Copilot adapter

Read `/AGENTS.md`, then the nearest `AGENTS.md` for every touched path. Use `/docs/agent-map.yaml` to load only matched task documents. Canonical reusable workflows are under `/.agents/skills/`. This file contains no architecture or coding policy; follow the shared sources above.
